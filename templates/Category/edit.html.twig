{% extends 'base.html.twig' %}

{% block title %}
    Modification de la catégorie : {{ category.title }} - {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container">
        <h1>Modification de la catégorie {{ category.title }}</h1>
        {% include 'Category/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('app_category_index') }}" class="btn btn-secondary mt-4">Retour à la liste</a>
    </section>
{% endblock %}