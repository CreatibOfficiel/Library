<form action="{{ path('app_book_delete', { 'id': book.id }) }}" method="post" onsubmit="return confirm('Are you sure?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}" />
    <button class="btn btn-danger">Delete</button>
</form>