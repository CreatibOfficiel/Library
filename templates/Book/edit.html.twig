{% extends 'base.html.twig' %}

{% block title %}
    Modification du livre : {{ book.title }} - {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container">
        <h1>Modification du livre {{ book.title }}</h1>
        {% include 'Book/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('app_book_index') }}" class="btn btn-secondary mt-4">Retour à la liste</a>
    </section>


{% endblock %}